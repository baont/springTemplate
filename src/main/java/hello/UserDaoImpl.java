package hello;

/**
 *
 * @author tbg
 */
//@Repository
public class UserDaoImpl {
//extends JpaRepository<UserDetails, Integer> implements UserDao {
//    @PersistenceContext
//    private EntityManager entityManager;
//
//    @Override
//    public List<UserDetails> getUserDetails() {
////        Criteria criteria = entityManager.unwrap(Session.class).createCriteria(UserDetails.class);
////        return criteria.list();
//        List<UserDetails> listPersons = entityManager.createQuery(
//                "SELECT p FROM UserDetails p").getResultList();
//        return listPersons;
//    }
}
